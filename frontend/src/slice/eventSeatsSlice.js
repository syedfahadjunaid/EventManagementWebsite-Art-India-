import { createSlice } from "@reduxjs/toolkit";
// import { admin } from "../services/admin";

const initialState = {};

const eventSeatsSlice = createSlice({
  name: "eventSeats",
  initialState,
  reducers: {},
  extraReducers: {},
});

export default eventSeatsSlice.reducer;
